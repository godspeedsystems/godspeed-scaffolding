type: express
port: 3000

docs:
  endpoint: /api-docs
  info:
    title: Sample Hello World App
    version: 1.1.0
    summary: some http calls
    description: lets play with hello-world app

request_body_limit: 20000 #bytes
file_size_limit: 50000 #bytes

# authn:
#   jwt:
#     secretOrKey: mysecret
#     audience: mycompany
#     issuer: mycompany
# authz:
#   - id: check_user_role
#     fn: com.gs.transform
#     args: <%inputs.user.role === 'admin'%>

# on_request_validation_error: validations.request.standardResponse
# on_response_validation_error: validations.request.standardResponse
